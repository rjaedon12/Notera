import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Timeline Builder",
}

export default function TimelineBuilderLayout({ children }: { children: React.ReactNode }) {
  return children
}
